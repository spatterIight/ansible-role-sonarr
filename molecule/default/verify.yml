# SPDX-FileCopyrightText: 2026 spatterlight
#
# SPDX-License-Identifier: AGPL-3.0-or-later

---
- name: Verify
  hosts: all
  become: true
  gather_facts: false
  tasks:
    - name: Wait for sonarr service to become active (up to 180s)
      ansible.builtin.systemd:
        name: sonarr.service
      register: svc
      until: svc.status.ActiveState == 'active'
      retries: 36
      delay: 5

    - name: Assert sonarr service is active
      ansible.builtin.assert:
        that: svc.status.ActiveState == 'active'
        fail_msg: "sonarr.service not active ({{ svc.status.ActiveState }})"
        success_msg: "sonarr.service is active"

    - name: Wait for sonarr HTTP endpoint to respond (up to 10m)
      ansible.builtin.uri:
        url: "http://localhost:8989/"
        method: GET
        status_code: [200, 302]
        follow_redirects: none
      register: ep
      until: ep.status in [200, 302]
      retries: 120
      delay: 5

    - name: Assert sonarr HTTP endpoint responds
      ansible.builtin.assert:
        that: ep.status in [200, 302]
        fail_msg: "sonarr did not respond as expected ({{ ep.status | default('no response') }})"
        success_msg: "sonarr responded with {{ ep.status }}"

    - name: Show service status summary
      ansible.builtin.debug:
        msg:
          - "sonarr systemd: {{ svc.status.ActiveState }}"
          - "sonarr HTTP: {{ ep.status }}"
