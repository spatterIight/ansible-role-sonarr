# SPDX-FileCopyrightText: 2023-2026 Slavi Pantaleev
# SPDX-FileCopyrightText: 2025 spatterIight
# SPDX-FileCopyrightText: 2025, 2026 Suguru Hirahara
#
# SPDX-License-Identifier: AGPL-3.0-or-later

---
- name: Ensure Sonarr paths exist
  ansible.builtin.file:
    path: "{{ item }}"
    state: directory
    mode: "0750"
    owner: "{{ sonarr_uid }}"
    group: "{{ sonarr_gid }}"
  with_items:
    - "{{ sonarr_base_path }}"
    - "{{ sonarr_data_path }}"

- name: Ensure Sonarr support files created
  ansible.builtin.template:
    src: "{{ role_path }}/templates/{{ item }}.j2"
    dest: "{{ sonarr_base_path }}/{{ item }}"
    mode: "0640"
    owner: "{{ sonarr_uid }}"
    group: "{{ sonarr_gid }}"
  with_items:
    - env
    - labels
  register: sonarr_support_files_result

- name: Ensure Sonarr container image is pulled via community.docker.docker_image_pull
  when: devture_systemd_docker_base_container_image_pull_method == 'ansible-module'
  community.docker.docker_image_pull:
    name: "{{ sonarr_container_image }}"
    pull: always
  register: sonarr_container_image_pull_result_ansible_module
  retries: "{{ devture_playbook_help_container_retries_count }}"
  delay: "{{ devture_playbook_help_container_retries_delay }}"
  until: sonarr_container_image_pull_result_ansible_module is not failed

- name: Ensure Sonarr container image is pulled via ansible.builtin.command
  when: devture_systemd_docker_base_container_image_pull_method == 'command'
  ansible.builtin.command:
    cmd: "{{ devture_systemd_docker_base_host_command_docker }} pull {{ sonarr_container_image }}"
  register: sonarr_container_image_pull_result_command
  retries: "{{ devture_playbook_help_container_retries_count }}"
  delay: "{{ devture_playbook_help_container_retries_delay }}"
  until: sonarr_container_image_pull_result_command is not failed
  changed_when: "'Downloaded newer image' in sonarr_container_image_pull_result_command.stdout"

- name: Ensure Sonarr container network is created via community.docker.docker_network
  when: devture_systemd_docker_base_container_network_creation_method == 'ansible-module'
  community.docker.docker_network:
    enable_ipv6: "{{ devture_systemd_docker_base_ipv6_enabled }}"
    name: "{{ sonarr_container_network }}"
    driver: bridge
    driver_options: "{{ devture_systemd_docker_base_container_networks_driver_options }}"
  register: sonarr_network_creation_result

- name: Ensure Sonarr container network is created via ansible.builtin.command
  when: devture_systemd_docker_base_container_network_creation_method == 'command'
  ansible.builtin.command:
    cmd: >-
      {{ devture_systemd_docker_base_host_command_docker }} network create
      {% if devture_systemd_docker_base_ipv6_enabled %}--ipv6{% endif %}
      {{ devture_systemd_docker_base_container_networks_driver_options_string }}
      {{ sonarr_container_network }}
  register: sonarr_network_creation_result
  changed_when: sonarr_network_creation_result.rc == 0
  failed_when: sonarr_network_creation_result.rc != 0 and 'already exists' not in sonarr_network_creation_result.stderr

- name: Ensure Sonarr systemd service is present
  ansible.builtin.template:
    src: "{{ role_path }}/templates/systemd/sonarr.service.j2"
    dest: "{{ devture_systemd_docker_base_systemd_path }}/{{ sonarr_identifier }}.service"
    mode: "0644"
  register: sonarr_systemd_service_result

- name: Determine whether Sonarr needs a restart
  ansible.builtin.set_fact:
    sonarr_restart_necessary: >-
      {{
        sonarr_support_files_result.changed | default(false)
        or sonarr_systemd_service_result.changed | default(false)
        or sonarr_container_image_pull_result_ansible_module.changed | default(false)
        or sonarr_container_image_pull_result_command.changed | default(false)
      }}
