# SPDX-FileCopyrightText: 2026 spatterlight
#
# SPDX-License-Identifier: AGPL-3.0-or-later

---
name: molecule

on: [push, pull_request]  # yamllint disable-line rule:truthy

jobs:
  molecule:
    name: Molecule [${{ matrix.distro }}] [${{ matrix.scenario }}]
    runs-on: ubuntu-latest

    strategy:
      matrix:
        distro:
          - ubuntu2604
          - debian13
        scenario:
          - default
      fail-fast: false

    env:
      MOLECULE_DISTRO: ${{ matrix.distro }}
      PY_COLORS: "1"
      ANSIBLE_FORCE_COLOR: "1"

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6.0.2
        with:
          path: sonarr

      - name: Set up Python
        uses: actions/setup-python@v6.2.0
        with:
          python-version-file: sonarr/.python-version
          cache: pip
          cache-dependency-path: sonarr/molecule/requirements.txt

      - name: Install Python dependencies
        working-directory: sonarr
        run: pip install -r molecule/requirements.txt

      - name: Cache Ansible collections
        uses: actions/cache@v5.0.5
        with:
          path: ~/.ansible/collections
          key: ansible-collections-${{ hashFiles('sonarr/molecule/default/requirements.yml') }}

      - name: Install Ansible roles
        working-directory: sonarr
        run: ansible-galaxy role install -r molecule/default/requirements.yml

      - name: Install Ansible collections
        working-directory: sonarr
        run: ansible-galaxy collection install -r molecule/default/requirements.yml

      - name: Run Molecule
        working-directory: sonarr
        run: molecule test --scenario-name ${{ matrix.scenario }}
